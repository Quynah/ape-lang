# Multi-Language Semantic Equivalence
# Demonstrates: canonical semantics across language surfaces

task main:
    inputs:
        a: Integer
        b: Integer
    steps:
        - set total to 0
        - set temp_a to 0
        - set temp_b to 0
        if a > 2:
            - set temp_a to a
        if b > 2:
            - set temp_b to b
        - set total to temp_a



