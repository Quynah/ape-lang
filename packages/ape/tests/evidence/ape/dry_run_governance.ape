# Dry-Run Governance
# Demonstrates: deciding without mutating, safe analysis

task main:
    inputs:
        v1: Integer
        v2: Integer
    steps:
        - set total to 0
        - set temp_v1 to 0
        - set temp_v2 to 0
        if v1 > 15:
            - set temp_v1 to v1
        if v2 > 15:
            - set temp_v2 to v2
        - set total to temp_v1



